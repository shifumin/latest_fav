.container
  %p.lead 調べたいユーザーのスクリーンネーム (ID)を入力してください
  %p
    = simple_form_for @user_form, url: { action: :create }, html: { class: 'form-horizontal' } do |f|
      .form-group
        .form-row
          .col-3
            = f.input :screen_name, required: false, label: 'スクリーンネーム (ID)', placeholder: 'shifumin', class: 'form-control'
      = f.submit '調べる', class: 'btn btn-primary'
